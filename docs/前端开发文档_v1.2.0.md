# CardioGuard 360 前端开发文档 v1.2.0

## 📋 概述

本文档详细介绍 CardioGuard 360 前端项目的架构设计、技术选型、开发规范和核心功能实现。

---

## 🏗️ 项目架构

### 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4.x | 核心框架 |
| TypeScript | 5.3.x | 类型系统 |
| Vite | 5.0.x | 构建工具 |
| Element Plus | 2.5.x | UI 组件库 |
| Pinia | 2.1.x | 状态管理 |
| Vue Router | 4.2.x | 路由管理 |
| ECharts | 5.4.x | 数据可视化 |
| Axios | 1.6.x | HTTP 客户端 |
| Day.js | 1.11.x | 日期处理 |

### 目录结构

```
cardio-guard-frontend/
├── src/
│   ├── components/       # 公共组件
│   ├── layouts/          # 布局组件
│   │   └── MainLayout.vue    # 主布局(侧边栏+顶部导航)
│   ├── views/            # 页面组件
│   │   ├── Login.vue         # 登录页
│   │   ├── Dashboard.vue     # 数据看板
│   │   ├── Monitor.vue       # 实时监测
│   │   ├── Reports.vue       # 健康报告
│   │   ├── Devices.vue       # 设备管理
│   │   └── Alerts.vue        # 预警信息
│   ├── stores/           # Pinia Store
│   │   ├── user.ts           # 用户状态
│   │   └── monitor.ts        # 监测数据状态
│   ├── router/           # 路由配置
│   │   └── index.ts
│   ├── api/              # API 接口(待扩展)
│   ├── utils/            # 工具函数(待扩展)
│   ├── types/            # TypeScript 类型(待扩展)
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
├── public/               # 静态资源
├── index.html            # HTML 模板
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
└── package.json          # 依赖配置
```

---

## 🎨 核心功能实现

### 1. 用户认证系统

**技术实现:**
- JWT Token 存储在 localStorage
- Vue Router 路由守卫进行权限验证
- Pinia Store 管理用户状态

**关键代码:**
```typescript
// stores/user.ts
export const useUserStore = defineStore('user', () => {
  const token = ref<string>(localStorage.getItem('token') || '')
  const userInfo = ref<UserInfo | null>(null)
  
  async function login(username: string, password: string) {
    const response = await axios.post(`/api/auth/login?...`)
    token.value = response.data.data
    localStorage.setItem('token', token.value)
    await fetchUserInfo()
  }
  
  function logout() {
    token.value = ''
    userInfo.value = null
    localStorage.removeItem('token')
  }
})
```

**路由守卫:**
```typescript
// router/index.ts
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  
  if (to.meta.requiresAuth && !token) {
    next('/login')
  } else if (to.path === '/login' && token) {
    next('/')
  } else {
    next()
  }
})
```

### 2. 实时数据监测

**WebSocket 连接:**
```typescript
// stores/monitor.ts
function connectWebSocket(userId: number) {
  const token = localStorage.getItem('token')
  const wsUrl = `ws://localhost:8080/api/ws/health-monitor?userId=${userId}&token=${token}`
  
  ws = new WebSocket(wsUrl)
  
  ws.onmessage = (event) => {
    const data = JSON.parse(event.data)
    handleRealtimeData(data)
  }
}

function handleRealtimeData(data: any) {
  if (data.type === 'HEART_RATE') {
    heartRateData.value.push(data)
    latestData.value = data
    
    // 保持最近100条数据
    if (heartRateData.value.length > 100) {
      heartRateData.value.shift()
    }
  }
}
```

**实时更新 UI:**
```vue
<!-- views/Monitor.vue -->
<div class="heart-rate-value">
  {{ monitorStore.latestData?.heartRate || '--' }}
</div>

<v-chart :option="waveformOption" autoresize />
```

### 3. ECharts 数据可视化

**注册组件:**
```typescript
import { use } from 'echarts/core'
import { CanvasRenderer } from 'echarts/renderers'
import { LineChart, PieChart } from 'echarts/charts'

use([CanvasRenderer, LineChart, PieChart, ...])
```

**心率趋势图:**
```typescript
const heartRateChartOption = computed(() => ({
  tooltip: {
    trigger: 'axis',
    formatter: '{b}<br/>心率: {c} bpm'
  },
  xAxis: {
    type: 'category',
    data: monitorStore.heartRateData.map(d => 
      new Date(d.measureTime).toLocaleTimeString()
    )
  },
  yAxis: {
    type: 'value',
    name: '心率 (bpm)',
    min: 40,
    max: 180
  },
  series: [{
    name: '心率',
    type: 'line',
    smooth: true,
    data: monitorStore.heartRateData.map(d => d.heartRate),
    areaStyle: { /* 渐变填充 */ }
  }]
}))
```

**使用组件:**
```vue
<v-chart :option="heartRateChartOption" autoresize />
```

### 4. 响应式布局

**Element Plus Grid 系统:**
```vue
<el-row :gutter="20">
  <el-col :span="6">统计卡片1</el-col>
  <el-col :span="6">统计卡片2</el-col>
  <el-col :span="6">统计卡片3</el-col>
  <el-col :span="6">统计卡片4</el-col>
</el-row>
```

**自适应图表:**
```vue
<v-chart :option="chartOption" autoresize />
```

---

## 🔧 开发规范

### 1. 命名规范

**文件命名:**
- 组件文件: PascalCase (如 `MainLayout.vue`)
- 工具文件: camelCase (如 `dateFormat.ts`)
- Store 文件: camelCase (如 `user.ts`)

**变量命名:**
- 常量: UPPER_SNAKE_CASE
- 变量/函数: camelCase
- 类/接口: PascalCase
- 私有变量: _camelCase

### 2. 组件规范

**Composition API 优先:**
```vue
<script setup lang="ts">
import { ref, computed } from 'vue'

const count = ref(0)
const doubleCount = computed(() => count.value * 2)
</script>
```

**Props 类型定义:**
```typescript
interface Props {
  title: string
  count?: number
}

const props = withDefaults(defineProps<Props>(), {
  count: 0
})
```

### 3. TypeScript 规范

**严格模式:**
```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
```

**类型定义:**
```typescript
// types/user.ts
export interface UserInfo {
  id: number
  username: string
  realName: string
  role: 'PATIENT' | 'DOCTOR' | 'ADMIN'
  phone?: string
  email?: string
}
```

### 4. 样式规范

**使用 Scoped CSS:**
```vue
<style scoped lang="scss">
.component {
  .child {
    color: #333;
  }
}
</style>
```

**SCSS 变量:**
```scss
$primary-color: #409eff;
$success-color: #67c23a;
$warning-color: #e6a23c;
$danger-color: #f56c6c;
```

---

## 🚀 性能优化

### 1. 代码分割

**路由懒加载:**
```typescript
{
  path: '/dashboard',
  component: () => import('@/views/Dashboard.vue')
}
```

### 2. 组件优化

**使用 v-memo (Vue 3.2+):**
```vue
<div v-memo="[count]">
  {{ expensiveComputation(count) }}
</div>
```

**keep-alive 缓存:**
```vue
<router-view v-slot="{ Component }">
  <keep-alive>
    <component :is="Component" />
  </keep-alive>
</router-view>
```

### 3. 图表优化

**按需引入 ECharts:**
```typescript
import { use } from 'echarts/core'
import { LineChart } from 'echarts/charts'
// 只引入需要的组件
```

**防抖处理:**
```typescript
import { debounce } from 'lodash-es'

const updateChart = debounce(() => {
  // 更新图表
}, 300)
```

---

## 🧪 测试计划

### 单元测试 (Vitest)

```bash
npm install -D vitest @vue/test-utils jsdom
```

**测试示例:**
```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import Login from '@/views/Login.vue'

describe('Login', () => {
  it('renders login form', () => {
    const wrapper = mount(Login)
    expect(wrapper.find('input[type="text"]').exists()).toBe(true)
  })
})
```

### E2E 测试 (Cypress)

```bash
npm install -D cypress
```

---

## 📦 部署方案

### Docker 部署

**Dockerfile:**
```dockerfile
FROM node:18-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**构建镜像:**
```bash
docker build -t cardioguard-frontend:1.2.0 .
docker run -p 80:80 cardioguard-frontend:1.2.0
```

### Nginx 配置

```nginx
server {
    listen 80;
    server_name cardioguard.example.com;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket 代理
    location /ws {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 缓存静态资源
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## 🔐 安全考虑

### 1. XSS 防护

- Vue 自动转义文本内容
- 使用 `v-html` 时确保内容可信

### 2. CSRF 防护

- 使用 JWT Token 认证
- Token 存储在 localStorage

### 3. HTTPS

- 生产环境强制使用 HTTPS
- 配置 HSTS 头

---

## 📊 监控与日志

### 性能监控

```typescript
// 页面加载时间
window.addEventListener('load', () => {
  const timing = performance.getEntriesByType('navigation')[0]
  console.log('Page Load Time:', timing.loadEventEnd - timing.startTime)
})
```

### 错误追踪

```typescript
// 全局错误处理
app.config.errorHandler = (err, vm, info) => {
  console.error('Vue Error:', err, info)
  // 上报错误到监控系统
}
```

---

## 🎯 后续优化方向

### v1.3.0 规划
- [ ] AI 心电图分析模型集成
- [ ] 更多图表类型支持
- [ ] 数据导出功能 (PDF/Excel)

### v1.4.0 规划
- [ ] 移动端适配 (响应式设计)
- [ ] PWA 支持
- [ ] 离线数据缓存

### v2.0.0 规划
- [ ] 微前端架构
- [ ] 多主题支持
- [ ] 国际化 (i18n)

---

## 📝 总结

CardioGuard 360 前端项目采用现代化的 Vue 3 + TypeScript 技术栈,实现了:

✅ **完整的用户认证系统**  
✅ **实时心率监测和可视化**  
✅ **丰富的数据图表展示**  
✅ **响应式布局设计**  
✅ **规范的代码结构**  

项目已具备生产就绪条件,可安全部署使用!

---

<div align="center">

**CardioGuard 360 Frontend v1.2.0**

Made with ❤️ by CardioGuard Team

</div>
